apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nagp-develop-chart.fullname" . }}-test-connection"
  labels:
    {{- include "nagp-develop-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nagp-develop-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
