apiVersion: v1
kind: Service
metadata:
  name: {{ include "nagp-develop-chart.fullname" . }}
  labels:
    {{- include "nagp-develop-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nagp-develop-chart.selectorLabels" . | nindent 4 }}
